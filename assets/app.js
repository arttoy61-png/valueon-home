const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');if(menuBtn&&nav){menuBtn.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.textContent=open?'CLOSE':'MENU';});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuBtn.textContent='MENU';}));}
const slides=[...document.querySelectorAll('.slide')];const dots=[...document.querySelectorAll('.dot')];const prev=document.querySelector('.prev');const next=document.querySelector('.next');const pause=document.querySelector('.pause');const count=document.querySelector('.slide-count');let current=0;let timer;let paused=false;function render(i){if(!slides.length)return;current=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===current));dots.forEach((d,n)=>d.classList.toggle('active',n===current));if(count)count.textContent=String(current+1).padStart(2,'0')+' / '+String(slides.length).padStart(2,'0');}function start(){clearInterval(timer);if(!paused&&slides.length>1)timer=setInterval(()=>render(current+1),5200);}prev?.addEventListener('click',()=>{render(current-1);start();});next?.addEventListener('click',()=>{render(current+1);start();});dots.forEach((d,i)=>d.addEventListener('click',()=>{render(i);start();}));pause?.addEventListener('click',()=>{paused=!paused;pause.textContent=paused?'▶':'Ⅱ';pause.setAttribute('aria-label',paused?'재생':'일시정지');start();});render(0);start();